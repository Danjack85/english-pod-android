# 复制此文件为 keystore.properties 后填入你自己的签名信息。
#
# 生成密钥库：
#   keytool -genkeypair -v -keystore englishpod-release.jks -alias englishpod \
#     -keyalg RSA -keysize 2048 -validity 10000
#
# 注意：keystore.properties 与 *.jks 已在 .gitignore 中排除，请勿提交到仓库，
# 否则任何人都能签出可被系统当作正式更新安装的包。
#
# 若本文件不存在，release 构建会自动退回 debug 签名（能编译、可安装，但不适合发布）。

storeFile=englishpod-release.jks
storePassword=改成你的密钥库口令
keyAlias=englishpod
keyPassword=改成你的密钥口令
