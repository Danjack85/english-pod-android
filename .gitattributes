# 统一按 LF 存入仓库，检出时再按平台转换（core.autocrlf 已处理）。
# 显式声明可避免 Windows 上把 .bat 之类的行尾写进历史造成无意义 diff。
* text=auto eol=lf

*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做任何转换
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.apk binary
*.jks binary
*.keystore binary
*.jar binary
*.m4a binary
*.aab binary
